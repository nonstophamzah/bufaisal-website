# Phase 6.4 — Session start handoff (2026-05-11)

## What just shipped
Phase 6.3 merged in PR #40 (commit 67cbd29) and verified live on bufaisal.ae. Public site now reads display text from `published_*` columns via `resolvePublicItemFields()` in `src/lib/resolve-public-item-fields.ts`. Production tested on Hitachi refrigerator page — renders correctly.

## Resolver pattern (locked convention)
- File: `src/lib/resolve-public-item-fields.ts`
- Pattern: `item.published_X ?? item.legacy_X` (nullish coalescing, NOT `||`)
- Consumers: `src/app/item/[id]/page.tsx`, `src/app/item/[id]/item-detail-client.tsx`, `src/app/shop/shop-client.tsx`, `src/components/ItemCard.tsx`
- Resolver retires in 6.5 after legacy mirror is removed

## Legacy mirror status
Still active in `src/lib/admin-pending-publish.ts` lines 105-125. Retires in 6.5, after 6.4 ships and verifies live.

## Phase 6.4 scope (next)
Noon-style layout on `/item/[id]`:
- Spec table render from `published_spec_table` JSONB
- FAQ accordion from `published_faqs` JSONB
- Trust signals row from `published_trust_signals` JSONB
- Schema markup injection from `published_product_schema` and `published_faq_schema` JSONB
- Dedicated yellow Negotiate button (sticky on scroll per locked spec)

## 6.4 PREREQUISITE — DO NOT SKIP
`published_product_schema.image[]` currently contains placeholder tokens ("photo_1_url", "photo_2_url", etc.), NOT real Cloudinary URLs. AI prompt generates these because it doesn't know the URLs at generation time. Before 6.4 wires this column into rendered structured data, the publish helper (`src/lib/admin-pending-publish.ts`) must substitute placeholders → real URLs at approval time using the same image-URL chain as `src/lib/item-image.ts`. See Bufaisal-Decisions-Log entry 2026-05-11.

## Unrelated issues surfaced during 6.3 verification (NOT 6.3 problems)
1. `/admin/pending` broken on Vercel preview deployments. Likely missing env var (one or more of: ANTHROPIC_API_KEY, GEMINI_API_KEY, INTERNAL_API_SECRET, JOBS_SECRET, NEXT_PUBLIC_BASE_URL) not enabled for Preview scope. Works fine on production. Fix by auditing env vars in Vercel and enabling Preview scope for any production-only secrets.
2. Admin "Hidden" tab shows "No hidden items" even though two MacBook rows (45e82440, 01ff3138) have `is_hidden=true` in `shop_items`. Worth checking the admin's filter logic.

## Test data state
- 2 MacBook test rows hidden via `is_hidden=true` in `shop_items` before 6.3 merge: `45e82440-ed11-4009-8fc9-791da99065ce` and `01ff3138-63a7-4267-b782-0a41c0330022`. Stay hidden, can be deleted post-launch.
- 4 published rows currently visible on bufaisal.ae public site: Hitachi fridge (4cea5546), Siemens cooker (45a9e06b), Super General freezer (9ba77c73), and a Bufaisal storage shelf row not in the original 6.3 audit. Worth a quick integrity check of the shelf row before 6.4 work touches the same data path.

## Hamzah's preferences (re-stated)
- Direct, fast, informal communication
- One clear next step at a time, wait for confirmation
- Stress-test welcome — push back on bad plans
- Investigate and conclude directly rather than asking Hamzah to verify steps himself
- Verify with SQL/code reads before claiming state
