# Handoff — start Phase 6.3 (2026-05-11)

## State
On branch `main`, working tree clean, up to date with `origin/main` (tip `05c06ea`).

## What just shipped (Phase 6.0–6.2 complete)
- **6.0 — `ccc64a6`** — UUID leak fix on `/item/[id]`. `getItem` filters `is_published=true` and `is_hidden=false`, uses `.maybeSingle()`. PR #37.
- **6.1 — `4754bcb`** — Added 5 missing `published_*` columns to `shop_items`: `published_h1_title`, `published_geographic_anchor`, `published_image_alt_texts`, `published_product_schema`, `published_faq_schema`. Backfilled 5 published rows from `ai_*` counterparts. Migration: `supabase/migrations/021_add-phase6-published-columns.sql`. PR #38.
- **6.2 — `05c06ea`** — `buildPublishUpdate()` now writes all 16 `published_*` columns on every approval. Verified end-to-end with a fresh MacBook upload through `/team` → AI → `/admin/pending` approval. PR #39.

## What 6.3 will do
Switch the public product detail page (`/item/[id]`) and the listing/grid pages (`/shop`, `/`) to read from `published_*` columns instead of legacy columns (`item_name`, `description`, `seo_title`, etc.). This is the data-layer cutover for Phase 6 — **no visual change yet**. The Noon-style layout work (spec table, FAQ accordions, trust row, schema markup injection) is 6.4, separate PR.

## Read first, in order
1. `src/app/item/[id]/page.tsx` — product detail page, currently reads legacy columns.
2. `src/app/shop/page.tsx` and `src/app/shop/shop-client.tsx` — listing page.
3. `src/app/page.tsx` — homepage, duplicates the listing query (refactor out of scope for 6.3).
4. `src/lib/admin-pending-publish.ts` lines 105–125 — the "Phase 6 bridge" block. Retires after 6.3 verifies live, not in 6.3 itself.

## Subtleties carried forward
- **No Supabase CLI in this repo.** Migrations run by hand in the Supabase SQL Editor by Hamzah. Migration files are documentation + audit trail.
- **`.maybeSingle()` over `.single()`** is the locked convention when filters can legitimately exclude rows — prevents PGRST116 log spam.
- **Hamzah always runs SQL changes manually and approves before commit.** Show SQL first, never run destructive ops without explicit OK.
- **Stress-testing tone is welcome.** Push back on bad plans, surface conflicts before acting, don't silently revert flagged deviations to match a re-prompt's letter.
- **`pick()` helper at admin-pending-publish.ts:20** is the canonical fallback pattern for read paths at *publish time*. Public site reads should use `published_X` directly with a legacy-column fallback: `item.published_X || item.X`. The 5 currently-published rows have full `published_*` populated, so the fallback should never trigger in practice — keep it in place until 6.5 cleanup.
- **Schema markup is currently inline-generated** in `/item/[id]/page.tsx` around line 85 (Product + Breadcrumb JSON-LD). In 6.3, the source pivots: `published_product_schema` (JSONB) replaces the hand-built object, a new `<script>` block reads from `published_faq_schema`. Existing Breadcrumb logic stays inline.
- **`/shop` and `/` filter on `is_published`, not `status`.** Per locked decision in the Phase 6.0/6.1 review thread (Path A): keep `is_published` as the public filter. Don't change the listing query in 6.3 — the state-machine vs boolean cleanup is a separate small PR after Phase 6 ships.
- **RLS observation:** anon SELECT on `shop_items` is gated by `anon_read_published_shop_items` policy (tighter than the prior CLAUDE.md "wide open" claim suggested). Anon writes are blocked by absence of policy. Don't act on this in 6.3 — flagged for a future CLAUDE.md cleanup pass.
- **Parked branch `diesel/wip-2026-05-10`** holds in-progress diesel tracking work (commit `a67961b`). Do NOT touch unless Hamzah explicitly asks.

## What NOT to do in 6.3
- Do NOT change the Noon-style layout, add a spec table, add FAQ accordions, or build the trust row. That's 6.4.
- Do NOT remove the legacy column mirror from `admin-pending-publish.ts`. That retires after 6.3 is verified live.
- Do NOT touch `/admin/pending` — stable, shipped in Phase 5.
- Do NOT touch sacred routes: `/team`, `/admin`, `/appliance-tracker`, `/api/appliances`, `/api/gemini`.
- Do NOT migrate the 60 archived rows. They stay delisted.
