# Handoff — start Phase 6.2 (2026-05-10)

## State
On branch `main`, working tree clean, up to date with `origin/main` (tip `4754bcb`).

## What just shipped
- **6.0 — `ccc64a6`** — UUID leak fix on `/item/[id]`. `getItem` now filters `is_published=true` and `is_hidden=false`, uses `.maybeSingle()`. `is_sold` intentionally NOT filtered (sold items stay visible with the SOLD overlay for SEO). PR #37.
- **6.1 — `4754bcb`** — Added 5 missing `published_*` columns to `shop_items`: `published_h1_title`, `published_geographic_anchor`, `published_image_alt_texts`, `published_product_schema`, `published_faq_schema`. Backfilled the 5 currently-published rows from their `ai_*` counterparts. Migration: `supabase/migrations/021_add-phase6-published-columns.sql`. PR #38.

## What 6.2 will do
Update `src/lib/admin-pending-publish.ts` so the approve/quick-approve flows populate the 5 new columns on every future approval. Use the same `COALESCE(admin_*, ai_*)` pattern as the existing `published_*` fields where an admin override layer exists. The audit step will confirm which of the 5 new fields have `admin_*` counterparts and which source only from `ai_*`. No public-site changes; the read-path switch is 6.3.

## Read first, in order
1. `src/lib/admin-pending-publish.ts` — the publish helper being modified.
2. `supabase/migrations/021_add-phase6-published-columns.sql` — the columns it must mirror.
3. The "Phase 6 bridge — remove when public site reads published_* directly" block inside `admin-pending-publish.ts` — **DO NOT TOUCH in 6.2**, that legacy mirror retires in 6.3.

## Subtleties carried forward
- **No Supabase CLI in this repo.** Migrations are run by hand in the Supabase SQL Editor by Hamzah. Migration files are documentation + audit trail, not runnable state.
- **`.maybeSingle()` over `.single()` is the locked convention** when filters can legitimately exclude rows — prevents PGRST116 log spam from blocked or stale UUIDs.
- **Hamzah always runs SQL changes manually and approves before commit.** Show SQL first, never run destructive ops without explicit OK.
- **Stress-testing tone is welcome.** Push back on bad plans, surface conflicts before acting, don't silently revert flagged deviations to match a re-prompt's letter.
- **Parked branch `diesel/wip-2026-05-10`** holds in-progress diesel tracking work (commit `a67961b`, 3 files). Do NOT touch unless Hamzah explicitly asks.
