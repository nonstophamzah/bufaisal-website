# Post-Phase 6.6 — Next session handoff (2026-05-12)

## What just shipped (2026-05-12)

Three PRs landed on `main` over the course of one session, all verified against the 2026-05-12 production baseline (49 visible rows, 8-category map, 1 "hitachi" match):

- **Phase 6.5b.1** — PR #47 (commit `c8aa2c3` on `main`). Routed every public display read through `resolvePublicItemFields()` or canonical sources (`admin_*?? worker_*` / `ai_*` / `getShop()`). 5 row-based files touched: `marketplace-client.tsx`, `ItemCard.tsx`, `item-detail-client.tsx` (lingering reads), `api/feed/route.ts`, `constants.ts` (`buildWhatsAppUrl`). Also added 4 fields to `ShopItem` (`worker_condition_type`, `worker_condition_grade`, `admin_condition_grade`, `ai_barcode_extracted`).
- **Phase 6.5b.2** — PR #49 (commit `493f46d` on `main`). 5 PostgREST filter swaps across 4 SSR files: `page.tsx`, `shop/page.tsx`, `shop/shop-client.tsx`, `categories/page.tsx`. `.eq('category', …)` and `.or('item_name.ilike,brand.ilike,description.ilike')` → `published_*` equivalents. Pre-merge baseline 49/Hitachi-1/per-category map captured and matched post-merge.
- **Phase 6.6** — PR #50 (commit `3357e3d` on `main`). Deleted the 21-line legacy mirror block + the 3 dead `final*` const declarations in `admin-pending-publish.ts`. Also caught a Phase 6.5b.1 miss: `src/app/item/[id]/page.tsx:102` JSON-LD `sku: item.barcode` → `sku: item.ai_barcode_extracted`.

**Phase 6.5 is COMPLETE.** The public site reads canonically end-to-end. The legacy mirror columns on `shop_items` are no longer written by admin approval; the column drop is a future migration (Phase 9 or later).

## Phase 6.7+ scope — schema upgrades for SEO / AEO / GEO

The next architectural phase is widening the structured-data surface. Google Rich Results Test on `/item/4cea5546-1da6-48cb-b6c2-bf7a61232278` currently returns 5 valid schema items (Product snippets, Merchant listings, Breadcrumbs, FAQ, Organization) with deferred warnings now ready to be addressed.

### Recommended ordering

1. **Organization-level `aggregateRating`** — highest-visibility first win. Bufaisal has 2,390+ Google reviews across the 5 shops; lifting that into the Organization schema lights up gold stars next to "Bufaisal" in Google search and unblocks the Product / Merchant snippets warnings simultaneously (per CLAUDE.md: store-level reviews are the correct pattern for used-goods marketplaces — per-product reviews don't scale to catalog turnover). One-file change in `src/app/page.tsx` Organization schema block. Data source: Google Business Profile or scraped via a daily cron.
2. **`ItemList` JSON-LD on `/shop`** — currently emitted client-side in `shop-client.tsx:163-193` (the `dynamicSchema` `useMemo`). SSR-emit instead so crawlers see it in initial HTML. Move the JSON-LD generation up to `shop/page.tsx` and pass through; current client component can keep the visual list.
3. **`CollectionPage` schema on `/categories`** — adds machine-readable structure to the 8-category landing page. New JSON-LD block in `src/app/categories/page.tsx`.
4. **Merchant Listings `shippingDetails` + `hasMerchantReturnPolicy`** — unlocks Google Shopping eligibility. Goes into `augmentProductSchema()` at render time (already the pattern for sku, seller block, negotiable hint).
5. **Organization `postalCode` + `streetAddress`** — fills the remaining Organization warning. Tiny diff in `src/app/page.tsx`.

### Open follow-up issues filed 2026-05-12 (NOT phase-7 blockers)

- **[#48](https://github.com/nonstophamzah/bufaisal-website/issues/48)** — WhatsApp draft body shows replacement chars (�) instead of 📦 💰 📍 emojis. Pre-existing (verified by reading byte-level source against `main` pre-6.5b.1). Source bytes are correct UTF-8 `F0 9F 93 A6` etc. — cause is downstream (likely `encodeURIComponent` interaction with `wa.me` handler, or font/rendering on certain client OS). Low priority; product info still renders.
- **Unescaped user input in `.or(…ilike…)` interpolation.** Pre-existing in `page.tsx`, `shop/page.tsx`, `shop/shop-client.tsx`. PostgREST escapes URL operands but `,` or `)` in the search term could in theory confuse the filter parser. Low-severity hardening.
- **`condition_notes` has no `published_*` counterpart.** `/item/[id]` still reads `item.condition_notes` directly. Worker-controlled free-text field. Out of canonicalization scope; can stay legacy.
- **Image gallery fallback chain still reads `image_urls` / `thumbnail_url`.** `src/lib/item-image.ts` and the gallery fallback in `item-detail-client.tsx` — separate image-resolution concern, not part of the text canonicalization story.

## Operational state to be aware of (Hamzah Option A acceptance — 2026-05-12)

These were considered and explicitly accepted during the 6.6 audit:

- **Legacy `/admin` Live/Sold/Hidden tabs** render items approved after 6.6 with blank titles (`item_name=''`), empty categories (`category=''`), no condition badges (`condition=NULL`). Locked workflow is `/admin/pending`. Items remain identifiable by photo + price + shop badge. Migration of legacy `/admin` to canonical sources is deferred to Phase 9 (or a dedicated cleanup PR if it becomes operationally blocking).
- **Legacy `/admin` Edit form** (`action='edit'` at `src/app/api/admin/items/route.ts:180-199`) still writes to legacy columns only. Those edits no longer surface on the public site. Admins must use `/admin/pending` re-approval flow for edits that need to reach customers.
- **`admin_price_aed` and `admin_negotiable` overrides don't propagate to the public site.** Worker insert populates `sale_price` and `negotiable` once at submit time; the mirror block (now deleted) was the only writer that overwrote them with the admin override on approval. With the mirror gone, those overrides stop at the `admin_*` column. Public site continues to render the worker-submitted price + negotiable flag. Known issue; fix scoped for a future PR if/when admin overrides become an active workflow need.

## Files the next session must read first

1. `CLAUDE.md` — full read, especially the updated Conventions block and the "Phase 6 cutover snapshot (complete 2026-05-12)" section.
2. `docs/PHASE_STATE.md` — phase ledger.
3. `src/lib/augment-product-schema.ts` — the render-time SEO augmenter that Phase 6.7's Merchant Listings additions plug into.
4. `src/app/page.tsx` — Organization schema lives here for the aggregateRating + address fills.
5. `src/app/shop/shop-client.tsx:163-193` — the client-side `ItemList` `useMemo` that should move to SSR.

## Subtleties carried forward

- **`published_internal_link_targets` dangling override-key reference** at `admin-pending-publish.ts:142` (post-6.6 line). Pre-existing per the prior cutover snapshot — column never made it into a migration. PR #45 derives similar-item tiers from `published_brand`/`published_category`/`worker_shop_id` instead. Drop in a Phase 9 housekeeping PR.
- **`resolvePublicItemFields()` still load-bearing** for any pre-Phase-5 legacy rows. Retires only after the legacy text columns are physically dropped from `shop_items` (Phase 9 or later schema migration). Until then, the `?? legacy_X` fallback serves the few legacy rows that lack `published_*`.
- **No `admin_condition_type` column.** Phase 6.5b.1 added `worker_condition_type` to `ShopItem` but found no override layer for type. If admin ever needs to flip Used↔New at approve time, a future schema migration adds `admin_condition_type` and the standard `admin_X ?? worker_X` read pattern applies in `mapCondition()` and any future consumers.
- **Five inline JSON-LD sites** still duplicate the simpler `replace(/</g, '\\u003c')` script-injection escape (`src/app/layout.tsx`, `src/app/page.tsx`, `src/app/shop/page.tsx`, `src/app/shop/shop-client.tsx`, plus one more). The safer `escapeJsonLd()` helper at `src/app/item/[id]/page.tsx:73-77` is the locked pattern. Migrate when next touching those files; not urgent.
- **The 2026-05-12 production data baseline** is the gold-standard reference for any future cutover that touches public query filters: **122 total rows** in `shop_items`, of which **49 publicly visible** (`is_published=true AND is_sold=false AND is_hidden=false`), **0 pending** awaiting admin approval, **4 hidden** (`is_hidden=true`). Per-category map for the 49 visible: Appliances 6, Bedroom & Sleep 12, Everyday Essentials 1, Kids & Baby 1, Kitchen & Dining 9, Living Room & Lounge 16, Office Study & Fitness 2, Outdoor & Garden 2 (sum = 49). `ilike '%hitachi%'` on `published_item_name`/`published_brand`/`published_description` returns 1 match. Capture and verify against it the same way 6.5b.2 did.
